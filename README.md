# Vagrant and Ansible

## Bring the machine up and SSH into it
```bash
vagrant up
vagrant ssh


# To stop the VM
vagrant halt

# to delete the vm
vagrant destroy
```

## Install packages
```bash
   ansible-playbook main.yaml
```
